# arduino-k210-maixdock

OpenBlock device plugin for Sipeed K210 MaixDock (M1).

A control board based on the K210 RISC-V chip that only has basic functions and leads out all IO pins. Upload-mode only.
